# Common local development setup

The following tools are required regardless of approach:

- Docker
- Node.js (see `.nvmrc`)
- AWS CLI
- `awslocal` (LocalStack AWS CLI wrapper)

LocalStack is used to emulate AWS services such as Lambda and S3.
